# Codex workspace instructions

- When creating or editing Korean text files on Windows, save them as UTF-8 with BOM.
- After writing a Korean text/Markdown file, verify it with default PowerShell `Get-Content` without an explicit encoding.
- If a file was created through a patch and contains Korean text, convert it to UTF-8 with BOM before finishing.
